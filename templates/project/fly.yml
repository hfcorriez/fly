project:
  ignore:
    - injectjs/**
  imports:
    config: config
    paypalLib: lib/paypal

service:
  '@http':
    name: http
    port: 5000
    cors: true

http:
  getTasks:
    path: /api/getTasks
    cache: 200

cron:
  checkTime:
    time: "* * * * *"
