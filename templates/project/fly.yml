project:
  import:
    db: lib/db

service:
  $http:
    name: http
    port: 5000
    cors: true
    static:
      - prefix: /static
        root: static

http:
  getTasks:
    path: /api/getTasks
    cache: 200

cron:
  checkTime:
    time: "* * * * *"
