ignore:
  - injectjs/**


events:
  service:
    '@http':
      name: http
      port: 5001
      cors: true

  http:
    getTasks:
      path: /api/getTasks
      cache: 200

  cron:
    checkTime:
      time: "* * * * *"
